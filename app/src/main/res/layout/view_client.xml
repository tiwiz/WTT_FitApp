<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <FrameLayout
        android:layout_width="@dimen/connection_logo_side"
        android:layout_height="@dimen/connection_logo_side"
        android:layout_centerInParent="true">

        <ImageView
            android:id="@+id/googleFitIcon"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/ic_fit_big" />

        <ImageView
            android:id="@+id/errorIcon"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/ic_error_big"
            android:visibility="gone" />
    </FrameLayout>

    <Button
        android:id="@+id/connectToFitButton"
        style="?colorButtonNormal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="@string/client_view_connect_to_google_fit" />
</RelativeLayout>